# boboshell

FIXME: write description

## Usage

=> (use 'boboshell.core)        ; load library
=> (open-index "path/to/index") ; open your index
=> (pretty-print)               ; optional. toggle pretty-printing of output
=> (field-names)                ; list the field names in the open index.
=> (search "query")             ; query the index, guessing at a default field and no faceting
=> (search "query" {"field1" (FacetSpec.) "field2" (FacetSpec.)}
                                ; enable default faceting for specified fields.

## Installation

$ lein deps ; lein compile ; lein jar
$ lein repl
...

## License

FIXME: write
